# Examples

This directory holds non-normative example certificates, payloads, and message
flows that illustrate the protocol.

Examples must not contradict `../doc/protocol.md` or `../doc/ipc.md`. If an
example becomes stale, update or remove it rather than treating it as source of
truth.
