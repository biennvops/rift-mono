# Interoperability Tests

This directory is reserved for Windows <-> Android interop validation.

Interop tests are not implemented yet, so **Milestone M3 cannot be signed off**
based on evidence inside this repository.

Missing pieces include: an interop runner/playbook + recorded results for
pairing in both directions and edge cases (reject/cancel/timeout), plus restart
persistence and revoke behavior across platforms.
